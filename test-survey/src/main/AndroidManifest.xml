<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Androidtracker"
        android:name=".MyApplication"
        >
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Androidtracker">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <service
            android:name="com.example.survey_test_app.SurveyDataReceiver$SurveyDataReceiverService"
            android:foregroundServiceType="specialUse"
        />
        <service
            android:name="MyBackgroundController$ControllerService"
            android:foregroundServiceType="dataSync|health|specialUse|location|connectedDevice"
            android:exported="false">
        </service>
    </application>
</manifest>